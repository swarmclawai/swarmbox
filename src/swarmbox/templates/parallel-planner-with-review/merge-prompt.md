Merge reviewed SwarmBox branches into {{TARGET_BRANCH}}.

Before each merge:
- Confirm review fixes are committed.
- Merge one branch at a time.
- Run the relevant checks after each merge.

Commit the final merge result and finish with <promise>COMPLETE</promise>.
