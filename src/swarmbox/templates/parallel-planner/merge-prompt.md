Merge completed SwarmBox branches into {{TARGET_BRANCH}}.

Before each merge:
- Confirm the branch has a focused commit history.
- Run the relevant checks after merging.
- Resolve conflicts conservatively and keep unrelated code untouched.

Commit the final merge result and finish with <promise>COMPLETE</promise>.
