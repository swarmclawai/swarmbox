You are working in branch {{SOURCE_BRANCH}} targeting {{TARGET_BRANCH}}.

Task:
!`{{VIEW_TASK_COMMAND}}`

Implement the requested change in this branch. Keep the diff focused, run the most relevant tests, and commit the finished work.

When the work is complete, close the task with:
!`{{CLOSE_TASK_COMMAND}}`

Finish with <promise>COMPLETE</promise>.
